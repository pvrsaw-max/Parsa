# Parsayan V37 — Production Hardening

## هدف
این نسخه به‌جای افزودن بازی جدید، روی خرابی‌های تعاملی، جریان واقعی دکمه‌ها، تحمل خطا، موبایل و قابل‌تست‌شدن منطق «کی اینو گفت؟» تمرکز دارد.

## اصلاح اصلی: کی اینو گفت؟
- منطق ثبت/شروع/امتیازدهی از UI جدا و در `src/lib/quoteMachine.js` قابل تست شد.
- ورود نام‌ها تکراری‌ها را حذف می‌کند و حداقل سه نفر لازم است.
- ثبت جواب قفل ضد دوبار لمس دارد.
- بعد از هر ثبت، پیام واضح `✓ جواب ثبت شد` نمایش داده می‌شود و نفر بعد مشخص است.
- پس از جواب‌دادن همه، فرم ثبت کنار می‌رود و یک صفحه واضح «اعتراف‌ها جمع شد / شروع حدس‌ها» نمایش داده می‌شود؛ دیگر دکمه شروع کوچک و مبهم در هدر نیست.
- انتخاب گوینده، جواب کوتاه، سؤال معتبر و وضعیت شروع همگی Guard دارند.
- انتخاب اسم در مرحله حدس تا شروع تایمر ۱۵ ثانیه قفل است.
- Timeout فقط یک Miss ثبت می‌کند.
- کارت ناقص Recovery Screen دارد.
- اکشن‌های اصلی تمام‌عرض و مناسب لمس موبایل شدند.

## جلوگیری از Double Tap
- رگبار: داوری سریع پشت‌سرهم دیگر یک سؤال را چند بار حساب نمی‌کند.
- یک کلمه: امتیاز/رد/خطا قبل از Guard افزایش پیدا نمی‌کند و دوبار لمس یک کارت را دو بار ثبت نمی‌کند.
- کی اینو گفت: ثبت، شروع تایمر، Reveal و Next قفل کوتاه اکشن دارند.

## Crash safety
- `ErrorBoundary` سراسری اضافه شد. خطای غیرمنتظره UI به‌جای صفحه سفید، صفحه بازیابی با Reload و Start Clean نشان می‌دهد.
- State بازی اصلی همچنان در localStorage ذخیره می‌شود و Hydration/Invariantهای قبلی حفظ شده‌اند.

## UI/UX و Responsive
- textarea نیز به قرارداد فونت سراسری اضافه شد.
- Primary CTAها حداقل ارتفاع مناسب لمس دارند.
- Quote Game برای <=360px چینش دو ستونه کنترل‌شده برای انتخاب نام دارد.
- Feedback ثبت جواب، Completion State و CTA اصلی واضح‌تر شدند.
- Safe area، 320/340/390/430px و Landscape hardening نسخه‌های قبلی حفظ شدند.

## QA واقعی اجراشده
- `npm run test:audit`: PASS کامل.
- State machine simulation: 1000 بازی، 17,907 نوبت، 99,733 بررسی.
- V37 Production Hardening: 20/20 PASS.
- V36 Quote regression: 15/15 PASS.
- V34 Interaction Simulation: PASS.
- تمام تست‌های مستقل 6 بازی جانبی PASS.
- `node --check` روی data/engine/store/lib/vite config: PASS.

## Build
`npm run build` اجرا شد اما محیط فعلی `vite` را در node_modules ندارد و `npm install` نیز در این محیط timeout شد؛ نتیجه build: `vite: not found`. بنابراین Build محلی V37 تأییدشده اعلام نمی‌شود. GitHub Actions همچنان ابتدا `npm install`، سپس `npm run test:audit` و بعد `npm run build` را اجرا می‌کند.

## محدودیت تست
تست مرورگر فیزیکی iPhone/Safari در این محیط انجام نشده است. تست‌های responsive/Safari موجود source/contract based هستند.
