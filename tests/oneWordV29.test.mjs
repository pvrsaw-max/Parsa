import fs from'node:fs';let ok=0;const t=(n,c)=>{if(!c)throw new Error('FAIL '+n);console.log('PASS '+n);ok++};const d=fs.readFileSync('src/data/oneWordGame.js','utf8'),u=fs.readFileSync('src/screens/OneWordGame.jsx','utf8'),m=fs.readFileSync('src/screens/PartyModes.jsx','utf8'),c=fs.readFileSync('src/styles/app.css','utf8');t('120 cards',(d.match(/"id":"ow\d{3}"/g)||[]).length===120);t('unique ids',new Set(d.match(/ow\d{3}/g)||[]).size===120);t('12 categories',new Set([...d.matchAll(/"category":"([^"]+)"/g)].map(x=>x[1])).size===12);t('45 second rule',d.includes('ONE_WORD_SECONDS=45'));t('forbidden words',([...d.matchAll(/"forbidden":\[([^\]]+)\]/g)]).length===120);t('difficulty metadata',(d.match(/"difficulty":/g)||[]).length===120);t('dedicated route',m.includes('OneWordGame')&&m.includes('mode==="oneword"'));t('countdown',u.includes('setInterval')&&u.includes("setPhase('result')"));t('timeout locks round',u.includes('x<=1'));t('host correct',u.includes('const got=')&&u.includes('setScore'));t('free pass',u.includes('const pass=')&&u.includes('setPasses'));t('foul tracking',u.includes('const foul=')&&u.includes('setStrikes'));t('shuffle deck',u.includes('shuffle(oneWordCards)'));t('forbidden rendered',u.includes('card.forbidden.map'));t('one-word rules copy',u.includes('دقیقاً یک کلمه'));t('mobile css',c.includes('.owClock')&&c.includes('@media(max-width:390px)'));console.log(`ONE WORD V29 ${ok}/16`);
