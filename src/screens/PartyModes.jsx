import React,{useState}from"react";import GameShell from"../components/GameShell";import RapidFire from"./RapidFire";import ChainGame from"./ChainGame";import OneWordGame from"./OneWordGame";import DirectorGame from"./DirectorGame";import ThreeSecondGame from"./ThreeSecondGame";import QuoteGame from"./QuoteGame";
const MODES=[{id:"rapid",title:"رگبار",icon:"⚡",desc:"۴۵ ثانیه سؤال پشت سؤال؛ رد کردن آزاد."},{id:"chain",title:"زنجیره",icon:"🔗",desc:"هر نفر ۵ ثانیه؛ تکرار یعنی زنجیره پاره شد."},{id:"oneword",title:"یک کلمه، بیشتر نه",icon:"☝️",desc:"فقط یک کلمه راهنما؛ کلمات قرمز ممنوع."},{id:"director",title:"کارگردان",icon:"🎬",desc:"صحنه، نقش، محدودیت و یک پیچ داستانی وسط اجرا."},{id:"three",title:"سه‌ثانیه",icon:"3️⃣",desc:"سه جواب معتبر؛ دقیقاً سه ثانیه واقعی."},{id:"quote",title:"کی اینو گفت؟",icon:"💬",desc:"جواب‌های واقعی جمع؛ ۱۵ ثانیه برای پیدا کردن گوینده."}];
export default function PartyModes({back}){const[mode,setMode]=useState(null);if(mode==="rapid")return <RapidFire back={()=>setMode(null)}/>;if(mode==="chain")return <ChainGame back={()=>setMode(null)}/>;if(mode==="oneword")return <OneWordGame back={()=>setMode(null)}/>;if(mode==="director")return <DirectorGame back={()=>setMode(null)}/>;if(mode==="three")return <ThreeSecondGame back={()=>setMode(null)}/>;if(mode==="quote")return <QuoteGame back={()=>setMode(null)}/>;return <GameShell><button className="backBtn" onClick={back}>→ برگشت</button><section className="modeHero"><span className="eyebrow">PARSAYAN SIDE GAMES</span><h1>بازی رو عوض کن، جمع رو نه.</h1><p>شش بازی مستقل؛ هرکدام قانون، بانک و ریتم خودش را دارد. قبل از شروع هر بازی، قوانین همان‌جا توضیح داده می‌شود.</p></section><div className="modeGrid">{MODES.map(m=><button key={m.id} onClick={()=>setMode(m.id)}><i>{m.icon}</i><span><b>{m.title}</b><small>{m.desc}</small></span><em>←</em></button>)}</div></GameShell>}
